on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  NODE_VERSION: '18.x'
  DATABASE_URL: ${{ secrets.DATABASE_URL }}

jobs:
  test:
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: testenv
      run: |
        echo "vars DATABASE_URL=${{ vars.DATABASE_URL }}"
        echo "sec DATABASE_URL=${{ secrets.DATABASE_URL }}"
        echo "NODE_VERSION=${{ env.NODE_VERSION }}"
        echo printenv
        echo env

#  build:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v3
#
#    - name: Set up Node.js
#      uses: actions/setup-node@v3
#      with:
#        node-version: ${{ env.NODE_VERSION }}
#        cache: 'yarn'
#
#    - name: yarn prisma generate, push
#      run: |
#        echo "DATABASE_URL=${{ secrets.DATABASE_URL }}"
#        yarn
#        yarn prisma migrate deploy
